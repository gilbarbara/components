import { Meta } from '@storybook/addon-docs';
import { Box, Grid, Group, Jumbo, Paragraph } from '../../src';
import { spacing } from '../../src/modules/theme';

<Meta title='Intro/Spacing' />

<Jumbo>Spacing</Jumbo>

<Grid display="inline-grid" gap={spacing.xl} templateColumns="repeat(3, 1fr)">
  {Object.keys(spacing).map(d => (
    <Group>
      <Box width={spacing.xxl}>
        <Paragraph>{d}</Paragraph>
        <Paragraph mt={0} size="mid" variant="gray">{spacing[d]}</Paragraph>
      </Box>
      <Box height={spacing.xxl} variant="primary" width={spacing[d]} />
    </Group>
  ))}
</Grid>
