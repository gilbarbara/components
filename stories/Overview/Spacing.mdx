import { Meta } from '@storybook/blocks';
import { Unstyled } from '@storybook/blocks';

import { Box, Grid, Spacer, Jumbo, Paragraph } from '../../src';
import { spacing } from '../../src/modules/theme';

<Meta title="Overview/Spacing" />

<Unstyled>
  <Jumbo>Spacing</Jumbo>

  <Grid display="inline-grid" gap={spacing.xl} templateColumns="repeat(3, 1fr)">
    {Object.keys(spacing).map(d => (
          <Spacer key={d}>
            <Box width={spacing.xxl}>
              <Paragraph>{d}</Paragraph>
              <Paragraph mt={0} size="sm" variant="gray">{spacing[d]}</Paragraph>
            </Box>
            <Box bg="primary" height={spacing.xxl} width={spacing[d]} />
          </Spacer>
        ))}
  </Grid>
</Unstyled>
